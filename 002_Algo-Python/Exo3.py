ch=input("donner un texte")
c_sp=["!",",",";","?","."]
ch1=""
def verification(ch):
    for i in ch:
        ch1=""
        if i not in c_sp:
            ch1=ch1+i
    return ch1

l=verification(ch)

def majusculepoint(ch):
    for i in range(ch):
        if "A"<=ch1[0]<="Z"and ch1[-1] in c_sp:
            return  True
        
l=majusculepoint(ch)

def len(ch1):
    for i in ch1:
        len(ch)==25
    return len(ch1)

